apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: bookstore-dev
  labels:
    app: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
  - name: http
    port: 8086
    targetPort: 8086
    protocol: TCP
