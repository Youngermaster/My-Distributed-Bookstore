apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: bookstore-dev
  labels:
    app: api-gateway
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
  selector:
    app: api-gateway
