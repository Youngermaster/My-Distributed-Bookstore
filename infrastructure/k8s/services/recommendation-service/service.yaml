apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: bookstore-dev
  labels:
    app: recommendation-service
spec:
  selector:
    app: recommendation-service
  ports:
  - name: http
    port: 8089
    targetPort: 8089
    protocol: TCP
