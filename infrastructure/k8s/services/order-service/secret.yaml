apiVersion: v1
kind: Secret
metadata:
  name: order-service-secrets
  namespace: production
type: Opaque
stringData:
  # IMPORTANT: Replace these values in production
  # Use kubectl create secret or external secret management
  database-url: "postgresql://bookstore:CHANGE_ME@postgres:5432/orders_db?sslmode=require"
  rabbitmq-url: "amqp://bookstore:CHANGE_ME@rabbitmq:5672/"
