apiVersion: v1
kind: Secret
metadata:
  name: user-service-secrets
  namespace: production
type: Opaque
stringData:
  # IMPORTANT: Replace these values in production
  # Use kubectl create secret or external secret management
  database-url: "postgresql://bookstore:CHANGE_ME@postgres:5432/users_db?sslmode=require"
  jwt-secret: "CHANGE_ME_TO_SECURE_SECRET_AT_LEAST_32_CHARS_LONG"
