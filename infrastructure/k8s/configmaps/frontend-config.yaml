apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: bookstore-dev
data:
  # Empty string will make the frontend use relative URLs (same domain as frontend)
  # This works when API Gateway and Frontend are both exposed via LoadBalancer
  VITE_API_URL: ""
